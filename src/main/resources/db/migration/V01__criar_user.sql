                       CREATE TABLE users (
                       id BIGSERIAL NOT NULL PRIMARY KEY,
                       name VARCHAR(100) NOT NULL,
                       age INTEGER NOT NULL
                       );
